# Security
- Dry-run by default; `--run` required to execute.
- Deny destructive patterns; optional root jail via IC_ROOT=/var/log.
- Redaction presets for email, ipv4, token via `--scrub`.
- All deps MIT/BSD; code MIT.
